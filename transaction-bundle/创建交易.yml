info:
  name: 创建交易
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/rest/payments"
  body:
    type: json
    data: |-
      {
        "type": "order",
        "identifier": "261741320077",
      //   "type": "topup",
      //   "identifier": 1,
        "gateway": "wxpay_jsapi"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
