info:
  name: 交易退款记录
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/rest/payments/:number/refunds"
  params:
    - name: number
      value: "907816931201290893"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
