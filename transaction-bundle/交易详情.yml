info:
  name: 交易详情
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/rest/payments/:number"
  params:
    - name: number
      value: "903119068604645900"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
