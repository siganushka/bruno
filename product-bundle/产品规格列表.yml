info:
  name: 产品规格列表
  type: http
  seq: 7

http:
  method: GET
  url: "{{host}}/api/products/:id/variants"
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "foo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
