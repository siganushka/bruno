info:
  name: 产品列表
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/rest/products"
  params:
    - name: lowestPrice
      value: "500"
      type: query
      disabled: true
    - name: highestPrice
      value: "1000"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
