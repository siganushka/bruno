info:
  name: 修改产品选项
  type: http
  seq: 10

http:
  method: PUT
  url: "{{host}}/api/product-options/:id"
  params:
    - name: id
      value: "3"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "foo111",
        "values": [
          { "text": "foo" }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
