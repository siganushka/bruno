info:
  name: 删除产品
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{host}}/api/products/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "foo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
