info:
  name: 产品详情
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/api/products/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
