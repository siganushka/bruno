info:
  name: 交易列表
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/rest/payments"
  params:
    - name: gateway
      value: alipay_page
      type: query
      disabled: true
    - name: state
      value: cancelled
      type: query
      disabled: true
    - name: dtype
      value: paymentorder
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
