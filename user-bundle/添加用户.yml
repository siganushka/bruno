info:
  name: 添加用户
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/api/users"
  body:
    type: json
    data: |-
      {
        "identifier": "wangmazi",
        "password": {
          "first": "123456",
          "second": "123456"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
