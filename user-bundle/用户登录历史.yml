info:
  name: 用户登录历史
  type: http
  seq: 10

http:
  method: GET
  url: "{{host}}/api/users/:id/logins"
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
