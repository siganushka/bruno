info:
  name: 上传文件
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/rest/media"
  body:
    type: multipart-form
    data:
      - name: rule
        type: text
        value: test_img
      - name: file
        type: file
        value:
          - D:\Backup\Documents\Downloads\2.png
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
