info:
  name: 区域详情
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/regions/:code"
  params:
    - name: code
      value: "610111"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
