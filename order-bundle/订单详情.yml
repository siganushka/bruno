info:
  name: 订单详情
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/rest/orders/:number"
  params:
    - name: number
      value: "261761335474"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
